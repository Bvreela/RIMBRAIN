# Deployment composition

Reserved for deployment manifests and platform instructions after component bootstrap. Deployment files must contain secret references only, never credentials. Windows is a required supported development/deployment path.
