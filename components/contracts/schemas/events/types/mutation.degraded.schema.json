{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/mutation.degraded.schema.json",
  "title": "events/types/mutation.degraded",
  "description": "Payload for event_type 'mutation.degraded' (feature 016; FR-1404/1410): a triggered pass could not reach a model and the rules-only terminal path produced no applicable remediation — recorded so a silent skip is impossible.",
  "type": "object",
  "additionalProperties": false,
  "required": ["reason", "detail"],
  "properties": {
    "reason": {
      "type": "string",
      "enum": ["cadence", "failure", "near_failure"],
      "description": "The trigger that fired the pass."
    },
    "detail": {
      "type": "string",
      "minLength": 1,
      "description": "Why nothing was proposed (endpoint error code, no defect findings, etc.)."
    }
  }
}
