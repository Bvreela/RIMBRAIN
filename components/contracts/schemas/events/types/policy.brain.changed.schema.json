{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/policy.brain.changed.schema.json",
  "title": "events/types/policy.brain.changed",
  "description": "Payload for event_type 'policy.brain.changed' (upstream kind 'brain_change'): {kind: skill|tool|watcher|notebook|journal|git, name?, action}.",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "action"],
  "properties": {
    "kind": {
      "enum": ["skill", "tool", "watcher", "notebook", "journal", "git"],
      "description": "Brain artifact family per upstream bus.py."
    },
    "name": { "type": "string", "minLength": 1, "maxLength": 256 },
    "action": { "type": "string", "minLength": 1, "maxLength": 64 }
  }
}
