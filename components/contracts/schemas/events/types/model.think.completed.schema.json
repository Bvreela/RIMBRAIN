{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/model.think.completed.schema.json",
  "title": "events/types/model.think.completed",
  "description": "Payload for event_type 'model.think.completed' (upstream kind 'think_end'): {notes, wake, calls, elapsed}.",
  "type": "object",
  "additionalProperties": false,
  "required": ["notes", "wake", "calls", "elapsed"],
  "properties": {
    "notes": { "type": "string", "maxLength": 65536 },
    "wake": { "type": "object" },
    "calls": { "type": "integer", "minimum": 0 },
    "elapsed": { "type": "number", "minimum": 0 }
  }
}
