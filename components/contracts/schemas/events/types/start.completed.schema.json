{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/start.completed.schema.json",
  "title": "events/types/start.completed",
  "description": "Payload for event_type 'start.completed' (native type, feature 008): Start Mode's exit contract held for every colonist — enclosed shelter, bed, replenishable food source, recreation — so the mode disengages and normal policy resumes.",
  "type": "object",
  "additionalProperties": false,
  "required": ["mode", "colonists", "conditions"],
  "properties": {
    "mode": { "const": "start" },
    "colonists": { "type": "integer", "minimum": 1 },
    "conditions": {
      "type": "object",
      "additionalProperties": false,
      "required": ["shelter", "beds", "food", "recreation"],
      "properties": {
        "shelter": { "type": "boolean" },
        "beds": { "type": "boolean" },
        "food": { "type": "boolean" },
        "recreation": { "type": "boolean" }
      }
    },
    "phases_completed": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Bootstrap phase ids that reached succeeded."
    }
  }
}
