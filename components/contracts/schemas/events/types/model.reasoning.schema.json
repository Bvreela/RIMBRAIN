{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/model.reasoning.schema.json",
  "title": "events/types/model.reasoning",
  "description": "Payload for event_type 'model.reasoning' (upstream kind 'reasoning'): {text} model thinking text, may be long. Envelope privacy classification is 'restricted'; omitted from shared profiles.",
  "type": "object",
  "additionalProperties": false,
  "required": ["text"],
  "properties": {
    "text": { "type": "string", "maxLength": 1048576 }
  }
}
