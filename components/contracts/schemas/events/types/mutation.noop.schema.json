{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/mutation.noop.schema.json",
  "title": "events/types/mutation.noop",
  "description": "Payload for event_type 'mutation.noop' (feature 016; FR-1410): a reflection pass ran to completion and proposed no change — the cadence counter resets and the rationale is recorded.",
  "type": "object",
  "additionalProperties": false,
  "required": ["reason"],
  "properties": {
    "reason": {
      "type": "string",
      "enum": ["cadence", "failure", "near_failure"],
      "description": "The trigger that fired the pass."
    },
    "rationale": {
      "type": "string",
      "description": "Model's stated reasoning for declining to mutate."
    }
  }
}
