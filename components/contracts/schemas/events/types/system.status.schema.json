{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/system.status.schema.json",
  "title": "events/types/system.status",
  "description": "Payload for event_type 'system.status' (upstream kind 'status'): game.status dict merged with {episode, seed, phase}. Open shape because upstream merges an arbitrary game.status dict.",
  "type": "object",
  "required": ["phase"],
  "properties": {
    "episode": { "type": "integer", "minimum": 0 },
    "seed": { "type": "string", "maxLength": 256 },
    "phase": {
      "enum": ["idle", "thinking", "playing", "reflecting", "loading"],
      "description": "Runner phase per upstream bus.py."
    }
  },
  "additionalProperties": true
}
