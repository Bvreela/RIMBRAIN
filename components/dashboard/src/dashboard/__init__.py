"""rimbrainagent-dashboard: settings UI + JSON API for the endpoint registry.

Feature 002 (specs/002-model-endpoints): thin file editor over
``profiles/endpoints.yaml`` and ``profiles/bindings.yaml`` via the runtime
package's public registry API. YAML stays authoritative (SC-003); secrets are
references only (FR-002, SC-005).
"""

__version__ = "0.1.0"
