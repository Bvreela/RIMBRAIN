# Integrations

External pinned dependencies live here after direct integration bootstrap. The current upstream baseline already contains its own nested RimBridge pointer. See the [repository topology](../specs/10-architecture/REPOSITORY-TOPOLOGY.md).
