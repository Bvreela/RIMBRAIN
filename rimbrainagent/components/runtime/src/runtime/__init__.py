"""rimbrainagent-runtime: endpoint registry, role bindings, probing, clients.

Feature 002 (specs/002-model-endpoints). YAML under ``profiles/`` is
authoritative; secrets are refs resolved at call time only.
"""

__version__ = "0.1.0"
