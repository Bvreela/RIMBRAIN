{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/bridge.event.ingested.schema.json",
  "title": "events/types/bridge.event.ingested",
  "description": "Payload for event_type 'bridge.event.ingested' (upstream kind 'ledger'): one game ledger event {kind, text, tick, day, ...}. The ledger subkind is preserved verbatim in payload.kind; extra upstream keys are preserved.",
  "type": "object",
  "required": ["kind"],
  "properties": {
    "kind": { "type": "string", "minLength": 1, "maxLength": 256, "description": "Upstream ledger subkind, preserved verbatim." },
    "text": { "type": "string", "maxLength": 65536 },
    "tick": { "type": "integer", "minimum": 0 },
    "day": { "type": "integer", "minimum": 0 }
  },
  "additionalProperties": true
}
