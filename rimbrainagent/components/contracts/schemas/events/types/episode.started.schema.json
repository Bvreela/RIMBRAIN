{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/episode.started.schema.json",
  "title": "events/types/episode.started",
  "description": "Payload for event_type 'episode.started' (upstream kind 'episode_start'): {episode, seed}.",
  "type": "object",
  "additionalProperties": false,
  "required": ["episode", "seed"],
  "properties": {
    "episode": { "type": "integer", "minimum": 0 },
    "seed": { "type": "string", "minLength": 1, "maxLength": 256 }
  }
}
