{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/system.log.schema.json",
  "title": "events/types/system.log",
  "description": "Payload for event_type 'system.log' (upstream kind 'log'): {text, ...}; upstream docstring is open-ended, so extra keys are preserved.",
  "type": "object",
  "required": ["text"],
  "properties": {
    "text": { "type": "string", "maxLength": 65536 }
  },
  "additionalProperties": true
}
