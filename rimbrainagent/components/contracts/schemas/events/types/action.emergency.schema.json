{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/action.emergency.schema.json",
  "title": "events/types/action.emergency",
  "description": "Payload for event_type 'action.emergency' (native type, feature 004; FR-308): a deterministic reflex fired - no model call was made or awaited. Emitted in addition to action.issued/completed for the matched rule.",
  "type": "object",
  "additionalProperties": false,
  "required": ["pack_revision", "rule_id", "template_id", "params", "outcome"],
  "properties": {
    "pack_revision": { "type": "string", "minLength": 1 },
    "rule_id": { "type": "string", "minLength": 1 },
    "template_id": { "type": "string", "minLength": 1 },
    "params": { "type": "object", "additionalProperties": true },
    "matched": {
      "type": "array",
      "items": { "type": "string" },
      "description": "All rule ids that matched this poll (priority-ordered)."
    },
    "outcome": { "enum": ["emergency"] }
  }
}
