{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/plan.rejected.schema.json",
  "title": "events/types/plan.rejected",
  "description": "Payload for event_type 'plan.rejected' (native type, feature 005; FR-402/403/405): a proposal failed at any stage — malformed model output, deterministic-gate violations, invalid mutation, or loop-level refusal. Zero writes accompany this event.",
  "type": "object",
  "additionalProperties": false,
  "required": ["plan_id", "pack_revision", "code", "message"],
  "properties": {
    "plan_id": { "type": "string" },
    "pack_revision": { "type": "string" },
    "code": {
      "type": "string",
      "minLength": 1,
      "description": "Named error: plan.malformed, plan.mutation_invalid, plan.scored_episode_active, plan.unresolved, ..."
    },
    "message": { "type": "string" },
    "endpoint_id": {
      "type": "string",
      "minLength": 1,
      "description": "Planning endpoint involved, when the proposal stage was reached."
    },
    "model": { "type": "string", "minLength": 1 },
    "usage": {
      "type": "object",
      "required": ["prompt_tokens", "completion_tokens"],
      "properties": {
        "prompt_tokens": { "type": "integer", "minimum": 0 },
        "completion_tokens": { "type": "integer", "minimum": 0 }
      },
      "additionalProperties": false
    },
    "violations": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
