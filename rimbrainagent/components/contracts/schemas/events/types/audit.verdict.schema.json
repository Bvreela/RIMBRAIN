{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rimbrainagent.dev/schemas/events/types/audit.verdict.schema.json",
  "title": "events/types/audit.verdict",
  "description": "Payload for event_type 'audit.verdict' (native type, feature 009; FR-804): the recorded result of one dedicated audit pass. Fail verdicts block promotion and name the failing check — never skipped.",
  "type": "object",
  "additionalProperties": false,
  "required": ["check_id", "domain", "verdict"],
  "properties": {
    "check_id": { "type": "string", "minLength": 1 },
    "domain": {
      "type": "string",
      "enum": ["code", "policy", "ux"],
      "description": "code = tests/corpus/validators; policy = pack invariants; ux = feed coverage + readable refusals."
    },
    "verdict": { "type": "string", "enum": ["pass", "fail"] },
    "subject": {
      "type": ["string", "null"],
      "description": "What was audited (candidate id, pack path, state dir); null for whole-system checks."
    },
    "reasons": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Plain-language findings; empty on pass."
    },
    "details": {
      "type": ["object", "null"],
      "additionalProperties": true,
      "description": "Check-specific structured evidence (counts, offending fields)."
    }
  }
}
