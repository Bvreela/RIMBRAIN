# Planned components

These directories are specification-only placeholders for future portable Git submodules. Do not add implementation here until the repository-bootstrap work package is approved and the corresponding remote exists. Component specifications live in `../specs/20-submodules/`.
